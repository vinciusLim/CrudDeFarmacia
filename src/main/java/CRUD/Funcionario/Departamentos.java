package CRUD.Funcionario;

public enum Departamentos {
    TI,
    VENDAS,
    ADMINISTRATIVO

    /* Quando for perguntar o departamento no main, é so pedir em formato de String utilizando o Departamentos
     + value.of("nome do departamento"), EX.: Departamentos.value.of("VENDAS"). se lembrar de usar uppercase
     na string.
     */
}
